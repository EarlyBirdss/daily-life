export interface ModifyProps {
  _id: object|number,
  parentId?: number,
  onClose: any,
  form: any,
};

export interface DetailProps {
  name: string,
  description: string,
  controllerType: string,
};
